# Generated by CloudMind
services:
  backend:
    build:
      context: ./.
      dockerfile: Dockerfile
    container_name: cloudmind-backend
    ports:
      - "8000:8000"
    restart: unless-stopped
